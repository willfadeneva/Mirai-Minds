"use client";
import Link from "next/link";import { useState } from "react";import { Menu, X, Sparkles } from "lucide-react";import { site } from "@/data/site";
export function Navbar(){const[open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 border-b border-white/70 bg-white/80 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link href="/" className="flex items-center gap-2 text-xl font-black"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-white text-midnight"><Sparkles className="h-5 w-5"/></span>{site.name}</Link><nav className="hidden gap-6 md:flex">{site.nav.map(([label,href])=><Link key={href} href={href} className="text-sm font-bold text-slate-600 transition hover:text-sky-600">{label}</Link>)}</nav><button onClick={()=>setOpen(!open)} className="md:hidden">{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t border-sky-100 bg-white/90 px-5 pb-5 md:hidden">{site.nav.map(([label,href])=><Link onClick={()=>setOpen(false)} className="block rounded-2xl px-4 py-3 text-slate-700 hover:bg-sky-50" key={href} href={href}>{label}</Link>)}</div>}</header>}
